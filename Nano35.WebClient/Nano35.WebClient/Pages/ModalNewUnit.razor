@if (this.Display)
{
    <div class="modal-background">
        <div class="modal-content card">
            <h4 class="card-header">Новое подразделение</h4>
            <div class="card-body">
                <EditForm Model="@model" OnValidSubmit="HandleValidSubmit">
                    <DataAnnotationsValidator />
                    <div class="form-group">
                        <label>Название Подразделения</label>
                        <InputText @bind-Value="model.Name" class="form-control" />
                        <ValidationMessage For="@(() => model.Name)" />
                    </div>
                    <div class="form-group">
                        <label>Адресс</label>
                        <InputText @bind-Value="model.Address" class="form-control" />
                        <ValidationMessage For="@(() => model.Address)" />
                    </div>
                    <div class="form-group">
                        <label>Номер телефона</label>
                        <InputText @bind-Value="model.Phone" class="form-control" />
                        <ValidationMessage For="@(() => model.Phone)" />
                    </div>
                    <div class="form-group">
                        <label>Формат работы</label>
                        <InputText @bind-Value="model.WorkingFormat" class="form-control" />
                        <ValidationMessage For="@(() => model.WorkingFormat)" />
                    </div>
                    <div class="form-group">
                        <label>Тип</label>
                        <InputSelect @bind-Value="model.UnitTypeId" class="custom-select">
                            <option selected disabled>Choose...</option>
                            @foreach (var item in _types)
                            {
                                <option value="@item.Id">@item.Name </option>
                            }
                        </InputSelect>
                        <ValidationMessage For="@(() => model.UnitTypeId)"/>
                    </div>
                    @* <div class="form-group"> *@
                    @*     <label>Регион</label> *@
                    @*     <InputText @bind-Value="model.RegionId" class="form-control" /> *@
                    @*     <ValidationMessage For="@(() => model.RegionId)" /> *@
                    @* </div> *@
                    @* <div class="form-group"> *@
                    @*     <label>Тип</label> *@
                    @*     <InputText @bind-Value="model.TypeId" class="form-control" /> *@
                    @*     <ValidationMessage For="@(() => model.TypeId)" /> *@
                    @* </div> *@
                    <button class="btn btn-primary"@onclick="Create">
                        @if (_loading)
                        {
                            <span class="spinner-border spinner-border-sm mr-1"></span>
                        }
                        Создать
                    </button>
                    @if (!string.IsNullOrEmpty(_error))
                    {
                        <div class="alert alert-danger mt-3 mb-0">@_error</div>
                    }
                </EditForm>
                <button class="btn btn-secondary"
                    @onclick="Hide">Close</button>
            </div>
        </div>
    </div>
}